import 'package:flutter/material.dart';

class LeaveAuthorization extends StatefulWidget {
  const LeaveAuthorization({super.key});

  @override
  State<LeaveAuthorization> createState() => _LeaveAuthorizationState();
}

class _LeaveAuthorizationState extends State<LeaveAuthorization> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("leave authorization"),
    );
  }
}
